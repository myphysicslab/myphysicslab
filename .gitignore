# .gitignore for jssimlab directory
# Put your private ignore rules in .git/info/exclude.
.DS_Store
/build
/docs
/debug
# adv-build contains advance-compiled build
/adv-build
# myConfig.mk is the user's customized copy of makeConfig.mk
myConfig.mk
# machine-name is used in test results, different on each machine
MachineName.js
TestHistory.txt
# closure-library is a symbolic link; needed when running HTML files from source code
closure-library
# .log files are generated by pdflatex
*.log
